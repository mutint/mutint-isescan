# Conda package specs, one per line, collected by the entry script and installed into
# env/tools with micromamba. See mutint-core/tools.txt for the contract.
#
# ISEScan, from bioconda. It shells out by bare name to hmmer, blastn and FragGeneScan,
# which the package depends on, so they land in env/tools/bin beside it and
# `runner.tool_environment` puts that directory first on the run's PATH.
#
# There is deliberately no per-platform machinery here or in the entry script: the entry
# script installs every component's tools in one micromamba solve, so a spec must have a
# build for every platform MutInt runs on. This one has had an osx-arm64 build since
# 1.7.3 h9e3228c_1 (September 2026); before that the line was commented out.
isescan
